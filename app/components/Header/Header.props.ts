export interface HeaderProps {
  /**
   * The title in the header
   */
  title: string;
  /**
   * Should the header have chevron to go back
   */
  withChevron?: boolean;
  /**
   * Right side Component
   */
  rightSideComponent?: any
}